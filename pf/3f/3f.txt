I did not test the threshold version because 5000 particles already worked perfectly without it (after multiplying self.Alpha by 3).

For the no resampling scenarios, they are as expected such that all particles just scatter around the map and fail to provide any sort of valid estimation.